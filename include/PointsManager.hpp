#include <Geode/Geode.hpp>
/*
class PointsManager {
public:
    static PointsManager* get() {
        static PointsManager instance;
        return &instance;
    }
    void setLastDeath(float lastDeath);
    float getLastDeath();
    void setLabel(cocos2d::CCLabelBMFont* label);
    cocos2d::CCLabelBMFont* getLabel();
    float getPoints();
    void setPoints(float points);
    void addPoints(float points);
    void updatePoints();

protected:
    float m_points = 0.f;
    float m_lastDeath = 0.f;
    cocos2d::CCLabelBMFont* m_label = nullptr;
};
*/